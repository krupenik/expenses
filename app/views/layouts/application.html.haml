!!!
%html{:xmlns => "http://www.w3.org/1999/xhtml"}
  %head
    %title= h(yield(:title) || "Untitled")
    = stylesheet_link_tag 'jquery-ui', 'tablesorter', 'application'
    = javascript_include_tag 'jquery.min', 'jquery-ui.min', 'jquery.tablesorter.min', 'application'
    = yield(:head)

  %body
    = render 'shared/header'
    #container
      - flash.each do |name, msg|
        %div{:id => "flash_#{name}"}= msg
      - if show_title?
        %h1= h(yield(:title))

      = yield
